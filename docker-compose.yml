version: "3.9"

services:
  raid-helper:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: raid-helper
    env_file:
      - .env
    ports:
      - "3333:3333"
    volumes:
      - raid-helper-data:/app/data
    restart: always

volumes:
  raid-helper-data:
